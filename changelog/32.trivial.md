- Fixed up tests so that they don't fail if optional dependencies are not provided
- Changed CI so that AR6 regression tests only run on pushes to main and tests of PyPI installs (i.e. out of regular CI)
