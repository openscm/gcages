Missing default tolerances which caused failures for the CMIP7 ScenarioMIP workflow if pint wasn't installed (picked up by the PyPI installation tests https://github.com/openscm/gcages/actions/workflows/install-pypi.yaml)
